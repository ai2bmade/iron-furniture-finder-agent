# ai2bmade — Claude Code 브리핑

이 문서는 SKILL.md들과는 성격이 다릅니다. SKILL.md는 OpenClaw 에이전트가
읽는 "실행 지침"이고, 이 문서는 **Claude Code가 이 프로젝트를 이어서 작업할 때
참고할 배경 설명**입니다. Claude가 대화(채팅)로 이 프로젝트를 설계하면서 나눈
논의와 결정 이유를 정리했습니다.

## 이게 뭐 하는 프로젝트인가

Josh(Peter님)이 해외 B2B 시장에서 잠재고객·파트너·유통사·경쟁사를 자동으로
찾는 시스템을 만들고 있습니다. 버티컬(산업군)마다 하나씩 "Finder Agent"라는
OpenClaw 스킬을 만들고, 이 여러 Finder Agent를 계속 늘려가면서 확장할
계획입니다. 그래서 버티컬마다 매번 처음부터 설계하는 대신, **"에이전트를
만드는 에이전트"(Finder Agent Factory)**를 먼저 만들어서, 이후로는 이
팩토리에게 몇 가지만 답하면 새 버티컬용 Finder Agent가 자동으로 나오게
했습니다.

## 전체 운영 구조 (배포 목표)

```
로컬(G드라이브) — Claude Code로 팩토리 실행/관리
   ↓ git commit/push
GitHub "ai2bmade" 저장소
   ↓ git pull
Hostinger VPS (OpenClaw 사전 설치됨)
   ↓ workspace/skills/ 자동 스캔
Telegram으로 트리거 → Finder Agent 실행 → 결과를 Telegram으로 회신
```

## 저장소 구조

```
ai2bmade/
├── CLAUDE.md                          # 이 문서
├── README.md                          # 사용자용 워크플로우 안내 (설치/배포 방법)
├── finder-agent-factory/              # 메타 에이전트 (에이전트를 만드는 에이전트)
│   ├── generate-finder-agent.js       # 실제 생성 로직 (Claude API 호출)
│   ├── package.json
│   ├── .env.example
│   └── templates/
│       └── finder-agent-methodology.md   # 고정 방법론 (사람이 읽는 기준 문서)
├── skills/                            # OpenClaw workspace skill root (Telegram/VPS 배포용)
│   └── handmade-metal-furniture-decor-finder/
│       └── SKILL.md                   # 1번 테스트 버티컬 (아래 히스토리 참고)
└── .claude/skills/                    # Claude Code 프로젝트 스킬 (대화형, 배포 대상 아님)
    ├── 수출바이어파인더/skill.md       # 링크드인 PDF → 등급 분류 → 엑셀/리포트
    ├── 유틸리티바이어소스리서치/skill.md
    └── 리테일바잉시그널모니터링/skill.md
```

`generate-finder-agent.js`와 `finder-agent-methodology.md`는 항상 같은
내용을 유지해야 합니다 (전자가 실제 동작 로직, 후자가 사람이 읽는 문서).
한쪽을 고치면 다른 쪽도 같이 고치세요.

### `skills/` vs `.claude/skills/` — 두 스킬 트리가 공존하는 이유

이 저장소에는 성격이 다른 두 스킬 계열이 있습니다. 원래 `E:\Finder_agent`에
따로 있던 "수출바이어파인더" 계열을 2026-08-15에 이 저장소로 병합하면서
구분이 생겼습니다.

- **`skills/` (OpenClaw 배포용)**: `finder-agent-factory`가 생성하는 버티컬별
  Finder Agent. git push → Hostinger VPS → Telegram 트리거 구조로 자동 실행됨.
  SKILL.md 형식(name/description/version frontmatter).
- **`.claude/skills/` (Claude Code 대화형 전용)**: 사람이 Claude Code로 직접
  세션을 열고 트리거하는 스킬. VPS로 배포되지 않고, 이 폴더에서 작업할 때만
  Claude Code가 자동 인식. 링크드인 PDF 파싱, 등급 분류(A~X), 엑셀/Full
  Report 생성처럼 사람의 판단·중간 확인이 필요한 무거운 워크플로우가 여기 속함.
  형식은 원본(`skill.md`, 소문자)을 그대로 유지 — OpenClaw SKILL.md로 변환하지
  않았음 (변환 시 PDF 파싱·등급 로직 등 세부 로직 손실 위험 때문).

두 트리는 서로 다른 목적(자동 배치 실행 vs 대화형 심층 리서치)이므로 앞으로도
통합하지 않고 병행 유지합니다. 새 스킬을 추가할 때 "정기적으로 자동 실행돼야
하는가"(→ `skills/`) "사람이 PDF 등 파일을 직접 넘기며 대화로 진행해야 하는가"
(→ `.claude/skills/`)로 위치를 판단하세요.

### 실제 작업 결과물(고객사 리서치 산출물)은 이 저장소에 없음

`.claude/skills/수출바이어파인더`로 실제 생성한 산출물(엑셀·PDF·Full Report,
예: 뷰티/전기장비 케이스)은 `E:\Finder_agent\Beuty\`,
`E:\Finder_agent\Electric_equipment\`에 그대로 남아 있습니다. 이 저장소는
GitHub(`ai2bmade`)로 push되므로, 실제 고객사·바이어 리스트가 담긴 민감한
결과물은 의도적으로 이 저장소 밖에 보관합니다. `E:\Finder_agent`는 이제
스킬 정의 없이 순수 작업 결과물 저장 용도로만 사용됩니다.

## 핵심 설계 원칙 (모든 Finder Agent에 고정)

대화 중 여러 차례에 걸쳐 다음 원칙들이 "모든 버티컬에 공통 적용"으로
확정되었습니다. 새 버티컬을 만들 때 이 원칙들을 다시 논의할 필요는 없습니다 —
이미 `finder-agent-methodology.md`와 `generate-finder-agent.js`에 박혀 있습니다.

1. **4대 조사 카테고리**: 엔드유저 / 잠재 파트너 / 잠재 디스트리뷰터 / 잠재 경쟁사
   (전부 B2B)
2. **디스트리뷰터 정의는 넓게**: 전통적 홀세일러·에이전트뿐 아니라, 전문샵·
   대형 옴니채널 리테일 체인·디자인 편집숍처럼 "대량 매입 후 재판매"하는
   리테일러도 디스트리뷰터로 본다. (엔드유저와는 "재판매 목적 대량 매입
   여부"로 구분)
3. **AI 답변엔진 질의 기반 발견**: ChatGPT/Perplexity 같은 AI 답변엔진에
   바이어들이 직접 질문해서 공급업체를 찾는 행동을 역이용 — 버티컬별 질의를
   만들어 답변에 등장하는 회사를 "AI 추천 발견"으로 태깅. 반복 등장하는
   회사는 우선순위를 높인다. (이 데이터는 나중에 별도로 만들 "우리 브랜드
   AEO/GEO 개선" 프로젝트의 벤치마크로도 재사용 예정 — 아직 미착수)
4. **경쟁사 네트워크 확장 조사(2차 발견)**: 경쟁사를 명단으로 끝내지 않고,
   경쟁사의 공개 레퍼런스·유통망·세일즈 담당자를 통해 그 경쟁사의 실제
   고객사까지 추적해서 엔드유저 후보로 편입. (LinkedIn 등 개인정보는 공개
   범위 내에서만, 대량 스크래핑 금지 — 이 주의사항은 매번 명시)

## 조건부 모듈 (버티컬 성격에 따라 켜고 끔 — 고정 아님)

아래 두 가지는 "모든 버티컬에 넣지 말 것"이라는 논의를 거쳐 조건부로
확정되었습니다. 팩토리가 인터뷰 중 판단하거나 사용자에게 물어봅니다.

1. **온라인 B2C 판매 가능 루트 조사**: 소비자가 온라인으로 직접 구매 가능한
   제품군(가구, 로봇 등)에서만 적용. 발전소 설비처럼 B2C가 불가능한
   버티컬에서는 생략.
2. **오너/대표 개인 리서치 심화**: 구매 의사결정이 오너 개인에게 집중된
   소규모 조직(디자인샵 등) 상대 버티컬에서만 적용. "발전소 대표를 찾아서
   뭐하나"는 지적이 나온 뒤 고정 항목에서 조건부로 격하됨 — 부서 단위
   의사결정을 하는 대형 조직 상대 버티컬에서는 의미가 없음.

## 1번 버티컬: 철제 감성 가구 — 설계가 바뀐 과정

이 버티컬은 대화 중 정의가 여러 번 바뀌었습니다. 최종 상태만 SKILL.md에
반영돼 있지만, 왜 이렇게 됐는지 이유를 알아야 다음 버티컬을 만들 때
비슷한 실수를 피할 수 있어서 정리합니다.

1. 처음엔 "핸드메이드 가구·철제 소품"으로 넓게 시작 → 가구 중심으로 좁힘
   (원목은 별도 버티컬로 분리하기로 함) → 소품(조명·플랜터 등)도 다시 포함
   → 최종적으로 "가구가 주력, 소품은 부수적" 정리
2. "핸드메이드"라는 표현 자체를 재정의: 실제 100% 수작업이 아니라 "철제
   소재 특유의 공예적·고급스러운 느낌"을 뜻하는 마케팅적 표현임을 명시.
   구체적 제품 라인(선반/책꽂이/테이블/수납장/**블록 조립형 모듈러 선반**)으로
   좁혀짐. "디자이너 가구"도 같은 축의 동등한 표현으로 추가.
3. 타겟 국가가 크게 확장됨: 북미·서유럽(원래 3개국)에서 → 서유럽·북유럽
   16개국, 동유럽 9개국, 중동/MENA 9개국, 남아공, 중남미 6개국, 일본, 대만,
   뉴질랜드까지 총 40개국 이상. "우선순위 없이 다 조사, 로테이션으로 순차
   진행"이 원칙으로 확정됨 (일부 국가를 배제하지 않음).
4. 디스트리뷰터 카테고리가 "홀세일러"에서 "전문샵/디자인 편집숍/미술관 부속
   편집숍"까지 확장됨. 특히 MoMA Design Store(뉴욕)를 계기로 전 세계
   유사 사례를 조사하도록 지침이 생겼고, 타겟 국가 중 **남아공(Zeitz
   MOCAA, 케이프타운)**과 **브라질(MASP Loja, 상파울루)**에 실제 사례가
   있음을 확인함. 단, 대형 옴니채널 체인은 브랜드 톤이 안 맞을 수 있어
   우선순위를 낮춤 — 디자인 편집숍이 우선.
5. "오너/대표 개인 리서치 심화" 모듈이 이 버티컬에 적용됨 — 디자인샵은
   대표 개인이 구매를 결정하는 경우가 많기 때문. (이 논의 과정에서 이
   모듈이 애초에 "고정"이 아니라 "조건부"여야 한다는 게 명확해짐 — 4번
   항목 참고)

## Claude Code에게 요청하는 작업

1. 이 저장소를 그대로 로컬(G드라이브 작업 폴더)에 두고 `git init` (또는
   기존 `ai2bmade` 원격 저장소가 있다면 clone 후 이 파일들을 병합)
2. `finder-agent-factory/`에서 `npm install` 실행, `.env.example`을 `.env`로
   복사 후 실제 `ANTHROPIC_API_KEY` 입력
3. `npm start`로 팩토리를 실행해서 실제로 잘 동작하는지 테스트 — 이미
   `skills/handmade-metal-furniture-decor-finder/SKILL.md`가 수동으로
   완성돼 있으니, 팩토리로 새 버티컬(예: 하우스홀드 로봇)을 만들어보면서
   생성 결과가 이 문서에 정리된 원칙(고정 4개 + 조건부 2개)을 잘 따르는지
   검증
4. 검증되면 git commit/push
5. Hostinger VPS에 SSH 접속해서 OpenClaw workspace 경로를 확인하고, README.md에
   있는 배포 절차(clone/pull, `skills/` 심볼릭 링크, Telegram 채널 연결)를
   진행
6. 다음 버티컬(로드맵: 하우스홀드 로봇 → 화장품 → 전기전자 → 발전소·변전소 →
   교육 콘텐츠 → 헬스케어 하드웨어 → AI 에이전트 시장) 진행 시, 조건부
   모듈(B2C 판매 루트, 오너 리서치) 적용 여부를 버티컬 성격에 맞게 판단할 것
   — 예를 들어 발전소·변전소는 두 조건부 모듈 모두 해당 없을 가능성이 높음

## 아직 하지 않은 것 (참고)

- "우리 브랜드 AEO/GEO 개선" 프로젝트는 별도로 만들기로 했으나 아직 착수
  전 (이번 대화의 AEO/GEO 논의는 어디까지나 "경쟁사가 이미 노출되는 지점을
  리서치 소스로 쓰는" 용도였고, 우리 브랜드 자체의 노출 최적화는 다른 작업)
- Telegram 봇 설정, OpenClaw 스프레드시트 연동(구글시트 등)의 정확한 도구/
  함수명은 실제 OpenClaw 콘솔에서 확인 필요 (SKILL.md에는 "무엇을 해야
  하는지"만 적혀 있고, "정확히 어떤 tool을 부를지"는 비어 있음)
